// Injected by build.mjs's esbuild `define` at build time — never assigned at
// runtime, so these are declarations only, not real variables.
declare const API_BASE_URL: string;
declare const WEBAPP_URL: string;
